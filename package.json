{
  "main": "index.js",
  "files": [
    "proto"
  ],
  "name": "${TFSCLIENT_DIST_NAME}",
  "version": "${TFSCLIENT_DIST_VERSION}",
  "dependencies": {
    "@grpc/proto-loader": "^0.5.0",
    "google-protobuf": "^3.10.0",
    "grpc": "^1.24.0"
  },
  "description": "${TFSCLIENT_DIST_DESCRIPTION}",
  "homepage": "${TFSCLIENT_DIST_URL}",
  "license": "${TFSCLIENT_DIST_LICENSE}",
  "author": "${TFSCLIENT_DIST_AUTHOR_NAME} <${TFSCLIENT_DIST_AUTHOR_EMAIL}>",
  "repository": "github:${TFSCLIENT_DIST_AUTHOR_ID}/${TFSCLIENT_DIST_NAME}"
}
